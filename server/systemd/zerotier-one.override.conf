[Service]
# Aguarda 10s após network-online.target para garantir que wlan0 está pronto
# (WiFi demora mais que ethernet - causa do bug de "OFFLINE no boot")
ExecStartPre=/bin/sleep 10

# Permitir reinicios ilimitados (Pi vai mudar de rede ao viajar)
StartLimitIntervalSec=0
RestartSec=15
